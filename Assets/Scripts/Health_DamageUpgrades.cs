using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Player;
using UnityEditor;
public class Health_DamageUpgrades : MonoBehaviour
{
    /*
    void Start()
    {
        
    }
    
    // everytime you get an Upgrade...
    
    // PlayerEntity.Instance.???_combatRoomsBeaten[ArrayUtility.IndexOf(LevelManager.Instance.???_combatRooms, this)] = true;
    // Player.IncreaseMaxDamage

    public void somethingHappens()
    {
        PlayerEntity.Instance.damageUpgradesCollected[ArrayUtility.IndexOf(LevelManager.Instance.damageUpgrades, this)] = true;
        PlayerCombat.IncreaseMaxDamage();
        
        PlayerEntity.Instance.healthUpgradesCollected[ArrayUtility.IndexOf(LevelManager.Instance.healthUpgrades, this)] = true;
        PlayerHealth.IncreaseMaxHealth();
    }

    */
}
